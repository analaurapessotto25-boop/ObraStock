{
  "framework": "vite",
  "installCommand": "cd \"Login Control\" && npm install",
  "buildCommand": "cd \"Login Control\" && npm run build",
  "outputDirectory": "Login Control/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
